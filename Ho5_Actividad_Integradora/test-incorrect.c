#define MAX 100
#define MAX 200

int global;
int global;

func suma(a,b) {
    int resultado;
    resultado = c + a;
    return resultado;
}

func suma(x) {
    return x;
}

func main() {
    int x;
    int y;
    int noUsada;
    x = w;
    suma(x);
    noExiste(x);
    if (condicion) {
        int z;
        z = x;
    }
    z = x;
    return noDeclarada;
}


